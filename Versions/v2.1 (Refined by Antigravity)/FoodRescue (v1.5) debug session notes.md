## 23/6/2026 FoodRescue debug session notes (v1.5) \[23/6/2026 , 24/6/2026 , 25/6/2026]

#### A resource for generating master prompts? Generate prompts and also answer questions...





\---

### Prompt used previously:

Act as an Expert Flutter \& Riverpod Mobile Engineer. I am building "FoodRescue", a mobile app for rescuing surplus F\&B items, and I need to generate the remaining UI screens and hook them up to my existing mock database providers.



Your goal is to write clean, modular, and DRY code that is ready for academic grading. Do NOT hardcode list data inside the UI files; always consume data via Riverpod `ref.watch()`.



\### 🏗️ CORE ARCHITECTURE \& ROUTING RULES

1\. State Management: Use `ConsumerWidget` or `ConsumerStatefulWidget` for all screens. Rely entirely on my existing workspace mock providers (e.g., `mockListingsProvider`, `mockReservationsProvider`, `mockUserProvider`) for data.

2\. Global Navigation: Do NOT put a `BottomNavigationBar` inside individual screen Scaffolds (this causes a double-navbar bug). Assume a root `MainLayout` handles the global bottom navbar, and only build the `body` content for the main tab screens (Home, Explore, Impact, Profile).

3\. Theming: Strictly use `Theme.of(context).colorScheme`. The app uses a "Stand-out Yellow" and clean white aesthetic. Primary buttons and accents should use the signature Yellow color. Use `Clip.antiAlias` on all Cards to prevent layout overflow crashes.



\### 📱 SCREEN-BY-SCREEN IMPLEMENTATION GUIDE



Please generate or update the following screens based on the wireframes in my proposal:



1\. Main Home Screen (`home\\\_screen.dart`)

\- Layout: A top app bar with a Calendar icon (routes to `ReservationsScreen`) and User Avatar.

\- Search \& Filter: Include a functional search bar and horizontal filter pills (All, Bakery, Produce).

\- Sections: Horizontal scrolling "For You" cards, a large Hero card for "Deal of the Day", and a vertical list for "Recently Added".

\- Data: Consume from the listings provider.

\- Action: Clicking a card pushes to `ItemDetailsScreen`.



2\. Discovery Map / Explore Screen (`discovery\\\_map\\\_screen.dart`)

\- Fix the current implementation: Bind the `FlutterMap` markers and the bottom `PageView` carousel to the real mock listings provider.

\- Filter Logic: Ensure tapping "Bakery" or typing in the search bar dynamically filters the map pins and carousel items.

\- Animation: When a user swipes the carousel or taps a pin, use a smooth animation loop (or tween) to glide the map camera to the new coordinates instead of jumping instantly.



3\. Item Details \& Booking Flow (`item\\\_details\\\_screen.dart`)

\- Layout: Large image header with a back button, followed by title, tags, distance, and "About this item" description.

\- Map Preview: A static, non-interactive mini-map container showing the pickup location.

\- Action: A bottom sticky "Reserve Now" button that triggers a `ReservationCreationModal` (Bottom Sheet).

\- Modal Logic: Allow adjusting quantity (+/-), selecting pickup time, toggling "Bring Your Own Container", and adding notes/tips. Clicking "Confirm" creates a CRUD record in the reservations provider and pushes to `ReservationConfirmationScreen`.



4\. Reservation Confirmation \& NETS QR (`reservation\\\_confirmation\\\_screen.dart`)

\- Layout: A success checkmark, order summary, and the crucial NETS QR Code container required for grading.

\- Data: Must display the specific `Reservation ID` and pickup timing.



5\. Reservations Management (`reservations\\\_screen.dart` \& `reservation\\\_details\\\_screen.dart`)

\- Layout: A TabBar switching between "Active" and "Past" reservations.

\- List Items: Show listing image, name, pickup time, and order ID.

\- Details View: When clicked, show the full reservation details, the NETS QR code, and provide two action buttons: "Update Pickup Timing" (triggers an update modal) and "Cancel Reservation" (triggers a delete CRUD action).



6\. Impact Dashboard (`impact\\\_dashboard\\\_screen.dart`)

\- Context: This satisfies the "Applied AI Feature" grading requirement.

\- Layout: Display metric cards for "Meals Saved" and "Total Weight".

\- AI Integration UI: Create a distinct section highlighting "Environmental Impact" (CO2 Reduced, Water Saved). Display the dynamic insight strings (e.g., `ai\\\_message`) generated by the AI evaluation of their rescues.

\- Include a simple bar chart (can use basic containers) for "Monthly Savings" and a "Community Leaderboard" list.



7\. Profile Screen (`profile\\\_screen.dart`)

\- Layout: User avatar, name, and email at the top.

\- Content: Personal info fields, a "Recent Rescues" summary list, and setting rows (Notifications, Payment, Security). Include a dark "Logout" button at the bottom.



\### 🎯 FINAL CHECKLIST BEFORE GENERATING

\- Are all buttons clickable with empty `() {}` closures or actual `Navigator.push` routes?

\- Is there ZERO hardcoded listing data in the UI widgets?

\- Are the NETS QR and AI Impact features explicitly visible to satisfy grading requirements?



Please process this and output the clean, refactored Dart files.



The prompt should be used as a guideline, your task is to build out the entire app and ensure that everything works.

VERY IMPORTANT: Refer to Part1\_Ho Chern Tung\_2501499D\_PC08.docx to understand how the app is supposed to work. The MBAP Project Guides can also help to establish a proper step-by-step process for developing the app but refer to the proposal to ensure that you are on task

\---





### Quick reflections:



Was quite impressed with Antigravity (Gemini's) implementation, primarily CRUD functions, so pushed app version to v1.5. Will attempt another major feat with Antigravity using the notes from v1.5 to establish v2.0.





### v1.5 debugging / testing notes:



#### Overall improvement notes:



Some screens do not match the reference designs, so make sure to follow them as closely as possible, also would like the design to be more polished where it should look like a very well designed mainstream app and not look "like an amateur made it" or "like the developer just learnt how to make apps". (general layout and design should be almost identical to design references, because marks are awarded for that, but design can still be refined to look more amazing and elegant.



Want to start with implementing the actual Firebase database, so need exact steps for how to do so. (try to populate the actual database with data already in the mock repositories so that the appearance of the interface doesn't change too drastically) Also reminder that the app is currently being built in a sandboxed experimental repository, will have to migrate to actual project submission folder after checks, how to do so elegantly, best possible way to do it?  Refer to proposal for database design, note that it may not be final because additional features in the future may require more entities.



Noticed an issue where the Reservations, Impact dashboard and Profile screens would just load infinitely when no reservations have been made (using mock database), is this supposed to happen? Ideally, what's supposed to happen after the actual database has been implemented is a proper indication or text that informs the user that there are currently no reservations and the Impact dashboard and profile screens should just load up without issues with the correct info.



!Check all screens with the html and image design references again!  (a lot of them are not accurate)

* Homepage -> Search bar looks a bit cropped out, fix this. Filter pills should have icons with text (in design reference) and tap again to deselect filter. The cards in the carousel seem to be cropped out a little bit at the top and bottom. The recently added section should also be a carousel identical to the one above. Could look nicer if horizontal scroll elements such as the filter pills and carouselS could scroll into the edges of the screen ; but still have some spacing in front, on the left prior to scrolling. (observed that a lot of mainstream apps do this, or just make a visible container around the carousels) Add "Reserve" button for each card, but not at the expense of the original price (both original and discounted price must be shown along with all the details already on each card).
* Discovery Map -> Fix the issue with the search bar, looks like there's a box inside it. Filter button should show or unshow filter pills, filter pills need to have tap again to deselect filter feature. Pins will be shifted to Singapore (in actual database). Each pin listing should also show the listing image above (see design references).
* Listing Screen -> Follow design references more closely. Map section should feature the actual map with pin coordinates. Reserve now model -> Follow design references more closely. Select pickup time should be a time scroller selector (view references).
* Reservation confirmation screen -> Need to follow design references more closely.
* View Reservations -> Could be more polished, follow design references more closely.
* Update Reservation -> Follow deign references more closely, update button should open up full update model, not just update pickup timing. Update Reservation Model -> Full update modal (check design references)
* Impact Dashboard -> Follow design references more closely. Data should be pulled from "users" table in actual database. Keep Gemini assistant.
* User Profile -> Follow design references more closely. User data should be pulled from "users" table in actual database. Enable logout button.





#### Notes about code:



Check through the entire codebase, see if code can be written to a better standard And improve accordingly (refer to rubrics for what is expected).

Noticed some deprecated code, try updating them.

Ensure no hard-coding, should be dynamic, use global theme etc ...





### Generate a resource document for better understanding of how the app works:



I would like a document that would explain the entire app completely, such as what fonts it uses and where icons etc are stored, should be easy to understand (someone completely new to this should be able to understand clearly)  Refer to documents MBAP Course Guide, MBAP Project Guide, Proposal to basically explain the entire codebase. -> must be sufficient to ace interview, check rubrics  Can consider formatting it or adding a section to help with QnA and concepts.





### Some other notes:



Ensure that correct mobile keyboards are used.





### Possible future features:



* 3D map
* Push Notifications
* Rewards system
* Review / Report system
* Custom loading screens



